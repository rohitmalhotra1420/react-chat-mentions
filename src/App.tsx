import { MentionsInput } from "./features/components/MentionsInput";

function App() {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <MentionsInput />
    </div>
  );
}

export default App;
